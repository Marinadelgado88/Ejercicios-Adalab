'use strict';
const numbers =[];
function getReversed100Numbers (number){
  for (let i = 0; i < 100; i++) {
    numbers.push(number+=1);
    numbers.reverse();
   console.log(get100Numbers);
  }
}
get100Numbers(Number());
console.log(numbers);