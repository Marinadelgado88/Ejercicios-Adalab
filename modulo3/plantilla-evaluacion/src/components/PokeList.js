import Pokemon from './Pokemon';

const Pokelist = (props) => {
  const item = props.data.map((pokemon, index) => {
  return (
    <div>
     <li>{club.name}</li>
    </div>
  );
}
}


export default Pokelist;
